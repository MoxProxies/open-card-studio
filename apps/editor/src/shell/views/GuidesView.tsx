import { Page } from "../Page";

/** Filled in by Phase 5 — see docs/PRODUCT_VISION.md. */
export function GuidesView() {
  return (
    <Page testId="page-guides" title="Guides" subtitle="How to print, cut and source card stock at home — plus design tips from the community.">
      <p style={{ padding: 16, margin: 0, fontSize: 13, color: "var(--cs-text-muted)" }}>Nothing here yet.</p>
    </Page>
  );
}
